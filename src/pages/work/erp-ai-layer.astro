---
import BaseLayout from "../../layouts/BaseLayout.astro";
import FlagshipPage from "../../components/FlagshipPage.astro";
import { getContent } from "../../i18n";

const c = getContent("en");
const f = c.flagship;
---

<BaseLayout lang="en" title={`${f.metaTitle} — ${c.profile.name}`} description={f.metaDescription}>
  <FlagshipPage lang="en" />
</BaseLayout>
