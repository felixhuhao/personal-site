# Resume Workspace

Place your resume `.tex` and `.cls` files in this folder.

Suggested layout:

```text
resume/
  resume.tex
  resume.cls
```

Build artifacts are ignored by the repository-level `.gitignore`.
